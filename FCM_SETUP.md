# Настройка Web Push уведомлений (Firebase Cloud Messaging)

## 📋 Обзор

Sparify использует Firebase Cloud Messaging (FCM) для отправки Web Push уведомлений пользователям.

## 🚀 Шаги настройки

### 1. Получение VAPID ключа из Firebase Console

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите ваш проект `sparify-4db02`
3. Перейдите в **Project Settings** (⚙️ иконка) → **Cloud Messaging**
4. Прокрутите вниз до раздела **Web Push certificates**
5. Если ключа нет, нажмите **Generate key pair**
6. Скопируйте **Key pair** (это ваш VAPID публичный ключ)

### 2. Обновление .env файла

Откройте файл `.env` в корне проекта и замените:

```env
VITE_FIREBASE_VAPID_KEY=YOUR_VAPID_KEY_HERE
```

на ваш реальный VAPID ключ:

```env
VITE_FIREBASE_VAPID_KEY=BCdx7WjN9eX... (ваш ключ)
```

### 3. Создание таблицы fcm_tokens в Supabase

Если вы ещё не создали таблицу, выполните следующий SQL в Supabase SQL Editor:

```sql
-- Таблица для хранения FCM токенов пользователей
CREATE TABLE IF NOT EXISTS fcm_tokens (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  token TEXT NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  UNIQUE(user_id, token)
);

-- Индекс для быстрого поиска по user_id
CREATE INDEX IF NOT EXISTS idx_fcm_tokens_user_id ON fcm_tokens(user_id);

-- Row Level Security (RLS)
ALTER TABLE fcm_tokens ENABLE ROW LEVEL SECURITY;

-- Политика: пользователи могут читать и изменять только свои токены
CREATE POLICY "Users can manage their own FCM tokens"
  ON fcm_tokens
  FOR ALL
  USING (auth.uid() = user_id)
  WITH CHECK (auth.uid() = user_id);
```

### 4. Настройка Supabase Edge Function

Edge Function `send-transaction-push` уже настроена и развёрнута. Она автоматически отправляет push уведомления при добавлении новых транзакций.

Убедитесь, что у вас есть Firebase Server Key:

1. Firebase Console → Project Settings → Cloud Messaging
2. Скопируйте **Server key** (это ваш Firebase Admin Key)
3. Добавьте его в Supabase Secrets:

```bash
npx supabase secrets set FIREBASE_SERVER_KEY="your_server_key_here"
```

### 5. Как это работает

```
┌─────────────┐
│  Браузер    │
│  (React)    │
└─────┬───────┘
      │
      │ 1. Запрос разрешения
      │    на уведомления
      ▼
┌─────────────────┐
│ usePushNotifications │
│     (хук)       │
└─────┬───────────┘
      │
      │ 2. Получение FCM токена
      │    через Firebase SDK
      ▼
┌─────────────────┐
│  Firebase       │
│  Messaging      │
└─────┬───────────┘
      │
      │ 3. Сохранение токена
      │    в Supabase
      ▼
┌─────────────────┐
│   fcm_tokens    │
│   (таблица)     │
└─────────────────┘

При добавлении транзакции:

┌─────────────┐
│ Транзакция  │
│  добавлена  │
└─────┬───────┘
      │
      │ Trigger
      ▼
┌───────────────────────┐
│ send-transaction-push │
│   (Edge Function)     │
└─────┬─────────────────┘
      │
      │ Читает FCM токен
      │ из fcm_tokens
      ▼
┌─────────────────┐
│  Firebase FCM   │
│  API (HTTP v1)  │
└─────┬───────────┘
      │
      │ Push уведомление
      ▼
┌─────────────┐
│  Браузер    │
│ (получает   │
│ уведомление)│
└─────────────┘
```

## 📝 Использование в коде

### Автоматическая регистрация

Push уведомления автоматически настраиваются при входе пользователя в `App.tsx`:

```tsx
const { isPermissionGranted, requestPermission } = usePushNotifications({
  userId,
  onNotificationReceived: (payload) => {
    // Обработка foreground уведомлений
    addNotification({
      type: 'deposit',
      title: payload.notification.title || 'Sparify',
      message: payload.notification.body || '',
    });
  },
});
```

### Ручной запрос разрешения

Если нужно запросить разрешение вручную (например, через кнопку в настройках):

```tsx
<button onClick={() => requestPermission()}>
  Включить уведомления
</button>
```

## 🧪 Тестирование

### 1. Проверка в браузере

1. Откройте приложение в браузере
2. Войдите в аккаунт
3. Разрешите уведомления, когда браузер попросит
4. Откройте Console (F12) и проверьте логи:
   - `FCM токен получен: ...`
   - `FCM токен успешно сохранён в Supabase`

### 2. Проверка в Supabase

Откройте Supabase Table Editor → `fcm_tokens` и убедитесь, что ваш токен сохранён.

### 3. Отправка тестового уведомления

Добавьте транзакцию в любую копилку и проверьте, что уведомление приходит.

## 🔧 Возможные проблемы

### "Не удалось получить FCM токен"

- Убедитесь, что VAPID ключ правильно настроен в `.env`
- Проверьте, что приложение работает по HTTPS (или localhost)
- Убедитесь, что Service Worker зарегистрирован: `navigator.serviceWorker.getRegistrations()`

### "Уведомления не приходят"

- Проверьте, что токен сохранён в таблице `fcm_tokens`
- Убедитесь, что Edge Function `send-transaction-push` развёрнута
- Проверьте логи Edge Function в Supabase Dashboard → Edge Functions → Logs

### "Service Worker не регистрируется"

- Убедитесь, что файл `public/firebase-messaging-sw.js` существует
- Проверьте путь к Service Worker (должен быть `/firebase-messaging-sw.js`)
- Для GitHub Pages убедитесь, что base в `vite.config.ts` установлен в `/`

## 📚 Дополнительные ресурсы

- [Firebase Cloud Messaging Documentation](https://firebase.google.com/docs/cloud-messaging/js/client)
- [Web Push API](https://developer.mozilla.org/en-US/docs/Web/API/Push_API)
- [Supabase Edge Functions](https://supabase.com/docs/guides/functions)
